#include<bits/stdc++.h>
using namespace std;

int findMin(vector<int> nums){
    int n = nums.size();
    if(n == 1) return nums[0];
    int low = 0;
    int high = n -1;
    int mid;
    while(low <= high){
        mid = low + (high - low)/2;
        
    }
}
int main(){
    vector<int> nums = {4,5,6,7,0,1,2};
    

    return 0;
}